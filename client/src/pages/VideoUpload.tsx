import { VideoUploader } from "@/features/video-uploading";
import { Container } from "@/components";

const VideoUpload = () => {
  return (
    <main>
      <Container>
        <VideoUploader />
      </Container>
    </main>
  );
};

export default VideoUpload;
